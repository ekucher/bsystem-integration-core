// Package repoguard holds no code. Its test asserts a property of the
// repository itself rather than of anything the platform runs: that no
// compiled or generated artefact is tracked in Git.
package repoguard
